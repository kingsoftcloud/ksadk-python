apiVersion: v1
kind: Service
metadata:
  name: {{ include "ksadk-docs.fullname" . }}
  labels:
    {{- include "ksadk-docs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "ksadk-docs.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
