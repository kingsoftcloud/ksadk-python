# {{PROJECT_NAME}}

Hermes AgentEngine 项目。

## 快速开始

```bash
cd {{PROJECT_NAME}}

# 1. 编辑 .env，填写模型配置
vim .env

# 2. 直接部署平台预置 Hermes runtime 镜像
agentengine hermes deploy

# 3. 打开 Hermes 管理 UI
agentengine hermes open

# 4. 进入 pod 内 Hermes 原生 TUI
agentengine invoke

# 5. 打开统一 hosted chat
agentengine hermes open --chat

# 6. 受限只读运维子命令
agentengine hermes exec <agent> -- status

# 7. Pairing 审批透传
agentengine hermes pairing <agent> -- list
```

## Runtime Contract

- `/` 反代到 Hermes dashboard 管理 UI
- `/chat` 由 AgentEngine hosted UI/router 处理
- `/v1/*` 反代到 Hermes OpenAI-compatible API server
- `/_ksadk/terminal/ws` 提供 `ks-terminal.v1` 原生 TUI/exec/pairing websocket

`runtime/app.py` 必须对 `/v1/*` 保持真正的 SSE 转发。不要把上游响应先读成完整 `content` 再返回，否则 hosted `/chat` 会表现成“长时间无输出后一次性吐完”。

## 自定义镜像

当前目录保留了一份 Hermes runtime 参考实现：

- `Dockerfile`
- `entrypoint.sh`
- `runtime/app.py`

默认 `agentengine hermes deploy` 会直接部署平台预置 Hermes 镜像。
如果你需要自定义 runtime，可先基于当前目录构建镜像，再执行：

```bash
agentengine hermes deploy --image <your-image>
```
