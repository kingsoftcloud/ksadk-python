# ops-assistant

## Purpose
A minimal preset skill mounted with the image to verify skill bootstrap works.

## Rules
- Report container health first.
- Prefer deterministic commands and include exact command lines.
- If a change may break startup, propose a rollback command.
