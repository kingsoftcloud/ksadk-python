# 用户偏好 / User Profile

这个文件用于记录用户级偏好和协作约定（非敏感）。

## 执行模式约定
- 默认执行模式是宽松模式（`OPENCLAW_EXEC_STRICT_MODE=false`）。
- 需要收紧权限时，显式设置 `OPENCLAW_EXEC_STRICT_MODE=true`。
- 宽松模式允许读取更多环境变量和执行能力，但不得在输出中泄露 token、密码、cookie 或私钥。

## 可记录内容
- 语言偏好、输出格式偏好、协作节奏。
- 常用命令风格和项目约定。

## 禁止记录
- 任意敏感信息明文（密钥、令牌、密码、会话凭据）。
