# 0.8.4 发布现场核对（2026-09-09）

本记录补充 `0.8.4-release-plan.md`，用于区分已经获得的证据和仍需执行的最终发布门禁。

## 已收敛的源码

- Python 候选基于最新 GitHub `main`，当前比 `github/main` 落后 0 个提交。平台资源 Runtime、Studio 前端与 Web 0.3.7 发布固定分别收敛为 `cd6dd87e`、`060b3813`、`5a267144`。
- Server 分支 `fix/plugin-artifact-ks3-delivery-20260908` 已提交并推送到 `9fa6a97`。它承载插件 KS3 制品交付、托管 Runtime 投影、会话 run identity 和 canonical interaction receipt；最终 Runtime 摘要将在重建后再次更新。
- Runtime Image 分支 `fix/codex-runtime-084-candidate` 已提交并推送到 `ba1f551`，固定 KsADK 0.8.4、Web 0.3.7、Codex 0.147.0，并提供从已准入镜像摘要快速刷新已审查 wheel 的构建路径。
- `@kingsoftcloud/ksadk-web@0.3.7` 已正式发布。GitHub `main`/tag `v0.3.7` 指向合并提交 `6a2814e`，npm registry integrity 为 `sha512-Js1Tk9Fo19QUgth1gXRBxZNEbtk5p81bMkX1ncdPRN4llldpfi3apAcaMxprsfI8V23cqf7pIKZIkeSDSAMZ8w==`。

## 已通过的验证

- Web 0.3.7 发布前门禁：47 个 Vitest 文件、344 项测试；130 项 Node 测试；lint、4 个构建、Conversation E2E 6 项、Demo E2E 1 项、npm pack 与干净安装 smoke 全部通过。正式包再次完成 registry 安装 smoke。
- Python Studio 云端共享 Web 与本地共享 Web 定向测试 32 项通过；Codex API 与 run spec 隔离回归 41 项通过。
- Studio React 使用正式 npm 0.3.7 同步 265 个静态文件，静态树 SHA-256 为 `067bad917730b9876998d09b8015b1cc4e2869ba47c08f9cd633693b7c93ed9a`；69 项 Node 测试、254 项 Vitest、TypeScript 和生产构建通过。
- Python 发布版本、文档、环境变量和样式门禁 63 项通过。
- 24 个受影响 Python 测试文件共 361 项通过、2 项因缺少可选的固定 DSH 测试安装而跳过；受影响 Python 文件 ruff 通过。
- Server 的 10 个受影响测试文件共 362 项通过，镜像摘要断言已与当前预发值同步。
- Runtime Image 的 0.8.4 固定值、摘要固定的 refresh 构建契约、脚本编译和构建命令展开检查通过；refresh 基础镜像摘要可从仓库读取。
- 使用正式 npm 0.3.7 的首轮 `make public-build-check` 已通过：运行时公共测试 26 项，twine wheel/sdist 检查通过，文件审计分别检查 1070/1309 项且 0 违规；该制品在后续预检发现修复项后废弃，最终候选必须重新构建。
- Phase 2 预检发现 Codex Provider 注册探测进程跨 asyncio 事件循环复用，以及 Provider 未复用 Studio 注入的 RuntimeExecutor。候选已改为有界注册探测、PluginHost 自有执行 sidecar 和单一 RuntimeExecutor；相关单元/集成测试 42 项通过、1 项显式 opt-in 跳过，真实浏览器三轮调度与连续会话通过。

## 真实链路结果

- 本地 Studio 8080 页面显示 12 个已安装插件和“全部 / KSADK 官方 / DSH / Codex”来源筛选；官方知识库、记忆库、Skill Center 与 Codex AgentProvider 可见。
- 记忆闭环已通过：会话 A 写入后完成抽取，会话 B 的 `load_memory` 命中 1 条；真实 MCP 写入约 6 秒后可检索。该结果不再回退为“读回为空”。
- 知识库真实 MCP 返回 2 项，Skill Center 返回 4 项。
- 第一次云端审批复验发现 Studio GET `/agentengine/api/v1/SubscribeRunEvents` 错误走本地 bridge 并返回 404。候选已改为按 `AgentId` 分派云端订阅，并把网关事件投影为 SSE；定向测试 32 项通过。
- 修复后，`SubmitInteraction` 和 `SubscribeRunEvents` 均返回 200；点击“批准并继续”后，同一页面、同一 run 自动继续接收事件，Skill 列举工具完成并返回 4 项，无需刷新。

## 当前剩余门禁

1. 提交 Provider 生命周期与 RuntimeExecutor 修复，推送 Python 候选并重新执行 `make public-build-check`。
2. 从新的最终 Python 候选 SHA 构建 wheel/sdist，并记录构建来源与摘要；此前从 `af6abeb5` 构建的 wheel 和 Runtime 镜像摘要 `sha256:e672de1566dfd1794686f01c07d8962c8ef74a321ce3fb3a3c1b865cc65d3195` 只作为废弃候选保留。
3. 使用新的最终 wheel 构建并推送 Codex Runtime 镜像，取得不可变 OCI digest；再更新 Server 预发 values 和断言。
4. 用最终镜像做插件恢复、知识库、记忆、Skill、审批、流式和用量联合回归。
5. 执行公开导出、凭证扫描、`make public-preflight`、`twine check` 和干净 wheel 安装 smoke。
6. 所有证据绑定最终 SHA 后，再请求 Python 0.8.4 的正式发布确认。

Runtime Image 仓库当前全量 `make audit` 还包含与本次候选无关的历史基线失败：旧 Hermes/OpenClaw 镜像断言、缺失 OTel 测试依赖以及旧记忆 flush 期望。0.8.4 镜像判断只引用上面的定向契约结果；这些基线问题需要独立修复，不能伪报为全量门禁通过。
