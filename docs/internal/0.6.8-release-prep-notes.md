# 0.6.8 文档优化准备笔记

> 本分支 `release/public-0.6.8` 为 0.6.8 发版准备的文档优化。按 `AGENTS.md` :83、:86 纪律，**未执行**版本号 bump、CHANGELOG 发版条目、tag、push github、PyPI 发布；这些需用户明确批准后再做。

## 本次改动

### 一、结构重组（docs/）

清理顶层散落的 19 个扁平 `.md`，按用途分入子目录（参考 veadk `content/docs/{cli,framework,references}` 的分层思路）：

- **guides/**：6 个用户指南（使用文档、Agent 上下文接入、记忆使用、知识库与记忆示例、DeepAgents、LangGraph 最佳实践）。
- **reference/**：6 个技术参考（环境变量、远程 runtime 接口、技术设计、OpenClaw 接口/网关/部署）+ `assets/`（OpenClaw 网关流程图与 HTML）。
- **release/**：`public-release-workflow.md`（`AGENTS.md` canonical 引用）。
- **internal/**：原有 6 个设计稿 + 工作区文件技术设计、平台可观测与用户反馈设计方案。
- **archive/**：5 个已完成/停用的规划稿（hosted-ui-refactor-plan、ksadk-iteration-plan-condensed、ksadk开源准备计划、prompt-driven-agent-creation-draft、veadk-benchmark-and-iteration-plan）+ 原有 `kb-memory/`、`versions/`、`workspace/`。
- 新增 `docs/README.md` 索引，说明 `docs/` 与 `main:public-docs/` 关系、各子目录用途与按角色阅读路径。

### 二、内容修复

- **环境变量文档补全**（`reference/ksadk环境变量参考.md`）：补齐 27 个 P0 coding agent 工具合并时漏更新的 env var，修复 `tests/test_config_env_registry.py` 失败：
  - sandbox（10）：`KSADK_SANDBOX_TTL_SECONDS` / `IDLE_TTL` / `MAX_SESSIONS` / `SESSION_ID` / `SYNC_MAX_*` / `ALLOW_POD_PROCESS_TOOLS` / `COMMAND_` / `COMMAND_CWD`
  - runtime 熔断（5）：`KSADK_MAX_TURNS` / `MAX_TOOL_CALLS` / `MAX_CONSECUTIVE_TOOL_FAILURES` / `MAX_CONSECUTIVE_APPROVAL_DENIALS` / `MAX_CONSECUTIVE_COMPACT_FAILURES`
  - tools（5）：`KSADK_TOOL_RESULT_MAX_CHARS` / `PERSIST_THRESHOLD_CHARS` / `PREVIEW_CHARS` / `DIR` / `SAFE_` 前缀
  - toolsets（2）：`KSADK_BUILTIN_TOOLS_MODE` / `PROFILE`
  - web（5）：`KSADK_WEB_SEARCH_PROVIDER` / `BASE_URL` / `API_KEY` / `SSRF_POLICY_JSON` / `USER_BACKEND_URL`
  - 更新陈旧头部（移除 `feat/skill-runtime` 工作树引用，注明由 env_registry 测试保证一致性）。
- **记忆使用指南补 LanceDB**（`guides/记忆使用指南.md` §5.2）：补 `lancedb` backend 行（0.6.7 新增进程内 OpenClaw 记忆插件，plugin id `memory-lancedb`，走 `MEMORY_BACKEND_MANIFEST` 不走 `--memory-system` CLI）。
- **断链修复**：重组后系统性修复 `guides/` ↔ `reference/` ↔ `internal/` 相对链接（约 15 处）；移除指向已删除 `deploy/openclaw-user-template/` 的预存断链（该目录已不在仓库）。
- **迁出路径引用修正**：`deploy/hermes/`、`deploy/openclaw/`、`deploy/openclaw-user-template/` 已于 commit `d2e37ec` 迁至 `agentengine-images` 仓库，全面修正仍引用旧路径的正文与图示：`reference/远程Agent运行时接口说明.md`（事实来源清单、运行时类型表、§7 Hermes 实现）、`reference/ksadk技术设计.md`（§3.3 托管资产 + 两处 mermaid 节点）、`reference/ksadk环境变量参考.md`（§12 模板变量指引）、`reference/openclaw一键部署指南.md`（文档说明 + docker 构建上下文注记）、`internal/工作区文件技术设计.md`（事实来源清单）。

### 三、引用同步

- `AGENTS.md:92`：`docs/public-release-workflow.md` → `docs/release/public-release-workflow.md`。
- `tests/test_config_env_registry.py`：`docs/ksadk环境变量参考.md` → `docs/reference/ksadk环境变量参考.md`（2 处）。

## 验证

- `uv run pytest tests/test_config_env_registry.py`：5 passed。
- 相对链接扫描：0 broken（`guides/` ↔ `reference/` ↔ `internal/` 跨目录链接全部解析通过）。
- `uv run mkdocs build --strict`：exit 0，0 error / 0 warning（仅 MkDocs 2.0 升级提示，与本仓库无关）。

## 四、public-docs + master docs 深度核对到 0.6.7（第二轮）

基于 8 feature 域审计（Checkpoint/Resume、Custom UI、LanceDB、CloudMonitor 双写、统一模型策略/fallback、Hosted 附件与会话分页、部署 env 边界、平台上下文/invocation_id），落地 49 文件 3135 增 203 删，不新建文件全部复用现有：

**public-docs/（双语 zh+en 对齐）**：
- `reference/environment-variables.{en,}.md`：补 CloudMonitor 11 变量子节、Unified Model Policy 节（fallback 语义/reasoning/thinking disable 注入）、UI 4 变量、Checkpoint Storage 小表、KS3/Image Registry/.env Boundary/Web UI Sync 小节。
- `guides/observability-tracing.{en,}.md`：新增云监控双写小节（双路径表 + 11 变量 + 占位示例 + 故障排查行），span attribute 补 invocation_id/account_id。
- `guides/memory-knowledge.{en,}.md`：补 OpenClaw memory backend 小节（lancedb 走 manifest 不走 CLI）。
- `guides/local-web-ui.{en,}.md`：新增 Custom UI bundle 小节 + 0.6.6 流式修复说明。
- `guides/web-ui-source.{en,}.md`：整篇重写为 npm-sync 口径（删 0.6.4 前 `npm run build:ksadk` 设计稿），与 zh 对齐。
- `reference/security-boundaries.{en,}.md`：补包边界（wheel 排除 web-ui/node_modules）+ 镜像仓库凭证边界（个人/企业/第三方 KCR）+ .env 构建上下文排除。
- `guides/build-and-package.{en,}.md`：新增发布门禁（public-build-check wheel 检查 + publish-pypi.yml Trusted Publishing）。
- `reference/remote-runtime-api.{en,}.md`：补 Checkpoint/Resume Actions + GetAgentUiBootstrap + ListSessions/ListSessionEvents 分页 + AttachmentContent ae-upload + RunAgent ModelMetadata。
- `reference/runtime-sessions-files.{en,}.md`：zh 新增 Checkpoint/Resume + 会话分页 + URI scheme 节；en 补 ResumeMode 能力说明。
- `guides/frameworks.{en,}.md`、`attachments-multimodal.{en,}.md`、`agent-context.{en,}.md`、`reference/openai-compatible-api.{en,}.md`、`reference/project-config.{en,}.md`、`reference/cli.{en,}.md`、`contributing/release.{en,}.md`、`guides/tools-and-skill-runtime.{en,}.md`、`runtime-products.{en,}.md`、`getting-started/configuration.{en,}.md`、`guides/runtime-architecture.{en,}.md`：按 plan 补对应 feature 字段/小节。
- 链接修复：memory-knowledge 双语原误链内部 `../../docs/` 改为 public-docs 内 `../reference/environment-variables.{en,}.md`；去掉易漂移的 `#fragment` 锚点链接（configuration/cli/attachments-multimodal 6 处）改纯文件链接。

**master docs/**（深度核对到 0.6.7）：
- `reference/远程Agent运行时接口说明.md`：ResumeRun 补 ResumeInstructionEnabled/ResumeInstruction + 409 禁用规则（checkpoint_not_resumable/resume_already_running）；CancelRun AgentId 改可选；ListSessionCheckpoints 补 CheckpointStatus 状态机；SubscribeRunEvents 补 5min 超时；GetAgentUiBootstrap 补 CustomUI/RuntimeCapabilities/CheckpointResumeCapability；RunAgent 补 Background/InvocationId/AccountId/ModelOptions + ModelMetadata；/v1/* 补 account_id；AttachmentContent 补 ae-upload 写回 cache；ExportWorkspaceZip 补路径过滤；ListSessions/ListSessionEvents 补分页字段。
- `reference/ksadk环境变量参考.md`：补 AGENTENGINE_MODEL_POLICY_JSON + Hermes/OpenClaw 模型策略 env + MEMORY_BACKEND_MANIFEST + CloudMonitor 双写叙事 + KS3_REGION。
- `guides/ksadk使用文档.md`：新增统一模型策略与 fallback 节 + ae-upload/AttachmentContent + 会话分页 + CustomUI + deploy --env/.env 边界 + KCR 凭证 + 长任务恢复流程。
- `guides/Agent 开发者上下文接入指南.md`：补 invocation_id/account_id + 0.6.5 三个安全读取入口 + ae-upload 统一解析 + ModelMetadata。
- `reference/openclaw一键部署指南.md`、`reference/ksadk技术设计.md`、`guides/LangGraph开发最佳实践.md`、`guides/记忆使用指南.md`：补 lancedb/平台上下文/checkpoint_ns 等。
- `ksadk/tracing/__init__.py`：修正 docstring 环境变量清单与代码对齐（补 10 个 CLOUD_MONITOR_* 变量、LANGFUSE_BASE_URL 优先顺序）。

## 未做（需用户批准）

- 版本号 bump（`ksadk/version.py` / `pyproject.toml` 0.6.7 → 0.6.8）。
- CHANGELOG `## [0.6.8]` 发版条目。
- tag、push github、PyPI 发布。
- `public-docs/` 同步到 GitHub `main` / Pages——本分支改动只在本地 + `origin`（内部 ezone），**未推 github**；公开同步需走 `docs/release/public-release-workflow.md` 流程（ezone 审核 → 用户批准 → 推 github main → 打 tag）。

## 后续可选迭代

- `reference/assets/` 的 OpenClaw html/svg/png 确认是否仍需要，或整合进对应 `.md`。
- 中文文件名英文化（可选，需评估对 public-docs 同步的影响）。
- public-docs 首页可借鉴 veadk 卡片导航（mkdocs-material `===` tab 或 def_list 模拟）。
- veadk `status: new` badge 风格（mkdocs 无原生，可用 admonition `!!! new` 标注 0.6.7 新内容）。

