# KsADK 0.6.2 Public Candidate Audit

> Date: 2026-06-04
> Scope: prepare a public `0.6.2` candidate from current `master` without publishing to GitHub or PyPI.

## Current Evidence

- Internal worktree: `master` at `bb1f38d fix: load public skills via premade API`.
- Current public baseline object: `github/main` / local `main` at `f66d2dc docs: default public docs to chinese`.
- Current `master` version files still report `0.6.0`:
  - `pyproject.toml`: `version = "0.6.0"`
  - `ksadk/version.py`: `VERSION = "0.6.0"`
  - `README.md`: `Current version: 0.6.0`
- Readable public worktree files report `0.6.1`:
  - `.worktrees/public-main/pyproject.toml`: `version = "0.6.1"`
  - `.worktrees/public-main/ksadk/version.py`: `VERSION = "0.6.1"`
  - `.worktrees/public-main/README.md`: `当前版本：0.6.1`
- `make public-sync-check` passes on current `master`.
- Initial `make public-worktree-status` failed because `.worktrees/public-main` had stale git metadata pointing to `/Users/xiayu/kingsoft/code/agent-sdk/ksadk-python/.git/worktrees/public-main`.
- `git merge-base HEAD github/main` does not produce a merge base, so direct `github/main..HEAD` diff is not a safe candidate scope. The public branch appears to use a reviewed clean-export history.
- After approval, `.worktrees/public-main` was recreated from `github/main`, and local branch `release/public-0.6.2` was created at `5b16884 feat: prepare ksadk 0.6.2 otel tracing candidate`.

## Release Rules Confirmed

Per `AGENTS.md` and `docs/public-release-workflow.md`:

- Do not edit `pyproject.toml`, `ksadk/version.py`, README version lines, or CHANGELOG release entries without explicit user approval.
- Do not run release or publish actions without explicit user approval.
- Do not directly merge `master` into GitHub `main`.
- Public sync must use a `release/public-x.y.z` candidate branch or equivalent reviewed public candidate.
- Public release requires `make public-preflight`, internal ezone review, GitHub push, release tag, and PyPI/GitHub Release only after approval.

## Candidate Commit Scope After 0.6.0

Use `a21e8e4 Release ksadk 0.6.0 UI and runtime updates` as the practical internal release boundary. Current `master` has these later commits:

| Commit | Theme | Public candidate treatment |
| --- | --- | --- |
| `4f3d13c` | Public premade skill loading | Candidate. Review Skill Service/public API wording and examples. |
| `57a34de` | OpenClaw user-template config reconciliation | Candidate if OpenClaw runtime assets are intended for public package. Needs template/security review. |
| `cdc6585` | OpenClaw config dashboard stabilization | Candidate if OpenClaw runtime assets are intended for public package. Needs bootstrap secret-path review. |
| `bbe405e` | Code build dependency packaging optimization | Strong candidate. Covers build/deploy UX and dependency packaging behavior. |
| `b26a19e` | Retry AICP inner endpoint for internal accounts | Candidate only after public wording is scrubbed. Avoid leaking internal endpoint details in README/PyPI docs. |
| `acf5a71` | Codify public release workflow | Candidate. This is release-process infrastructure. |
| `1100fca` | Pass Skill Runtime env to E2B commands | Candidate. Needs sandbox/env secret-handling review. |
| `bb1f38d` | Load public skills via premade API | Candidate. Needs public Skill API contract review. |

The OTel-first change is included in the local public candidate and should also be committed on internal `master`:

- `ksadk/tracing/setup.py`
- `ksadk/tracing/__init__.py`
- `tests/test_tracing_setup_otlp.py`
- `docs/ksadk环境变量参考.md`
- `docs/superpowers/plans/2026-06-04-otel-first-observability-release.md`
- `docs/superpowers/plans/2026-06-04-ksadk-0.6.2-public-candidate-audit.md`

## Public Candidate Construction Result

1. `.worktrees/public-main` was recreated from `github/main`.
2. Local branch `release/public-0.6.2` was created from `github/main`.
3. Reviewed public-safe patches were applied without bulk-merging `master`.
4. Public candidate version files and release notes were updated consistently:
   - `pyproject.toml`
   - `ksadk/version.py`
   - `README.md`
   - `CHANGELOG.md`
   - `README.en.md`
   - `README.zh-CN.md`
   - `uv.lock`
   - public docs and release workflow docs
5. Verification was run on the candidate branch:
   - focused public tests: `33 passed`
   - `python -m mkdocs build --strict`: pass, with a Material for MkDocs deprecation warning only
   - `git diff --check`: pass
   - `make open-source-audit`: `violations: 0`
6. Internal ezone branch push is still blocked:
   - `git push origin release/public-0.6.2` was rejected with a remote lock error.
   - `git push origin review/public-0.6.2-otel` printed a new-branch line but returned non-zero.
   - `git ls-remote --heads origin release/public-0.6.2 review/public-0.6.2-otel` did not show either branch afterward.
7. Only after internal review and explicit approval:
   - Push candidate to GitHub `main`.
   - Create and push `v0.6.2` with `make public-release-tag V=0.6.2`.
   - Publish PyPI/GitHub Release/Pages as approved.

## Verification Already Run

- `UV_CACHE_DIR=/private/tmp/uv-cache-ksadk uv run pytest tests/test_tracing_setup_otlp.py -q`
  - Result: `5 passed`
- `UV_CACHE_DIR=/private/tmp/uv-cache-ksadk uv run python -m py_compile /private/tmp/ksadk_custom_otel_observability_demo.py`
  - Result: pass
- `UV_CACHE_DIR=/private/tmp/uv-cache-ksadk uv run python /private/tmp/ksadk_custom_otel_observability_demo.py`
  - Result: pass
- `make public-sync-check`
  - Result: pass
- `make public-worktree-status`
  - Initial result: fail, stale public worktree metadata
- Public candidate focused tests in `.worktrees/public-main`
  - Result: `33 passed`
- Public candidate docs build in `.worktrees/public-main`
  - Result: pass, with a Material for MkDocs deprecation warning only
- `make open-source-audit` in `.worktrees/public-main`
  - Result: `checked: 1089`, `violations: 0`
- `git diff --check -- ksadk/tracing/setup.py tests/test_tracing_setup_otlp.py docs/superpowers/plans/2026-06-04-otel-first-observability-release.md`
  - Result: pass
- `git diff --check -- docs/ksadk环境变量参考.md ksadk/tracing/setup.py ksadk/tracing/__init__.py tests/test_tracing_setup_otlp.py docs/superpowers/plans/2026-06-04-otel-first-observability-release.md docs/superpowers/plans/2026-06-04-ksadk-0.6.2-public-candidate-audit.md`
  - Result: pass

## Current Blockers Before 0.6.2 Release

- Internal ezone remote branch push is blocked by remote locking; retry or repository-side lock cleanup is required before review can start.
- Full `make public-preflight` has not been run as one command on `release/public-0.6.2`; focused public tests, docs build, diff check, and open-source audit have passed.
- Current `master` worktree has many unrelated modified and untracked files, so any commit/release preparation must first isolate the intended changes.
- Public safety review is still required for AICP/internal endpoint wording, OpenClaw bundled assets, Skill Service public API examples, and sandbox env propagation.
- GitHub `main`, release tag, PyPI, GitHub Release, and Pages publishing still require explicit approval after internal review.
